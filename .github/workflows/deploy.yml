name: Deploy MkDocs to GitHub Pages
on:
  push:
    branches: [ gh-pages ]  # o master, según tu rama principal

permissions:
  contents: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build and deploy
        run: mkdocs gh-deploy --force
